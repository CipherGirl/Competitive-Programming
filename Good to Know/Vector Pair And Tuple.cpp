// #include <bits/stdc++.h>

// using namespace std;

// int main()
// {
// //    vector<pair<int, int> > myVec = { {-1, -1}, {2, 5}, {3, 10} };
// //    vector<pair<int,int>>::iterator it;
// //    for(int i = 0; i < myVec.size(); i++)
// //    {
// //        cout << myVec[i].first << ", " << myVec[i].second << endl;
// //    }
// //    vector<int> v = {1, 3, 5, 7, 9};
// //
// //    for (auto x : v)
// //    cout << x << ' ';

//    return 0;
// }


#include <bits/stdc++.h>
using namespace std;

struct point
{
    int poin;
};
struct cordinate
{
    int x,y;
};

int main()
{
    typedef vector< tuple<int, float, char> > my_tuple;
    my_tuple tl;
    // tl.push_back( make_tuple(21,20,65) );
    // tl.push_back( tuple<int, float, char>(69,67.69,'S') );
    // for ( int i = 0; i < tl.size(); ++i) {
    //     cout << get<0>(tl[i]) << " <-"<<i<<endl;
    //     cout << get<1>(tl[i]) << " <-"<<i<<endl;
    //     cout << get<2>(tl[i]) << " <-"<<i<<endl;
    // }
    // puts("");
    // cout << get<0>(tl[1]) << endl;
    // cout << get<1>(tl[1]) << endl;
    // cout << get<2>(tl[1]) << endl;

    vector<int, int> myVec;
    // vector<int, pair<int,int>>:: iterator itVec;
    // myVec.push_back(90,make_pair(23,90));
    // itVec=myVec.begin();
    // cout<<(*it).first<<endl;

    return 0;
}
